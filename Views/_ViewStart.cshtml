@* This file contains code that is executed at the start of each view's rendering process.
   It's used to set common properties for all views in the same directory and its subdirectories. *@

@{
    @* Sets the default layout page for all views in the application.
       The layout page specified here is "_Layout".
       This means that every view will use "_Layout.cshtml" as its default layout unless specifically overridden in a view. *@
    Layout = "_Layout";
}
